import React from "react"
import { memo } from "react"

export const ProcessList = memo(() => {
  return (
    <div
      style={{
        flex: 1,
        overflow: "auto",
        padding: 8,
      }}
    >
      哈哈
    </div>
  )
})